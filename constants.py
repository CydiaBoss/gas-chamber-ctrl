from pglive.kwargs import Axis

# General
APPID = "medal.gaschamber.uc"

# Axis
TIME_AXIS_CONFIG = {
    "orientation": "bottom",
    "units": "s",
    "tick_angle": -45,
    Axis.TICK_FORMAT: Axis.DURATION,
}